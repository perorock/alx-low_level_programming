#include "main.h"
#include <stdio.h>

/**
 * main - infinte loop
 * Return: 0
 */
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");
	i = 0;
i	/*
	 * while (i < 10)
	 * {
	 * Infinite loop - No increase of variable
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
